{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Preview Jekyll site",
      "type": "shell",
      "command": "bundle exec jekyll serve --livereload & JEKYLL_PID=$!; until curl -s http://127.0.0.1:4000/ >/dev/null; do sleep 0.5; done; open http://127.0.0.1:4000/; wait $JEKYLL_PID",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      },
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen",
        "instanceLimit": 1
      }
    }
  ]
}
