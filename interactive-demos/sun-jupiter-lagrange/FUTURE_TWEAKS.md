# Deferred tweaks

- Give the probe a more distinct color.
- Reduce Lagrange-point marker radii by roughly one half while retaining readable labels.
- Explain in the rotating-frame view that interior orbits advance relative to Jupiter, exterior orbits fall behind, and repeated encounters drive horseshoe reversals.
- Add an inertial-frame probe-speed readout in the diagnostics sidebar.
- Add a local escape-speed comparison. In this restricted three-body model it must be position-dependent, using the instantaneous barycentric two-primary potential and the current mass ratio, rather than presented as one universal number for the whole system.
- Explain that a probe with inertial speed above this local threshold is energetically unbound from the instantaneous two-primary potential; Jupiter commonly supplies the gravitational encounter that produces the speed increase.
- Replace the ambiguous Contours control with a clearly labelled, properly drawn zero-velocity-curve view for the current Jacobi constant, including an explanation that it maps permitted and forbidden regions rather than the probe's past or future trajectory.
- Remove the arbitrary half-radius inertial-frame reference circle. If reference circles remain, draw and label the actual barycentric orbital radii of the Sun and Jupiter.
- Make all frame geometry update with the mass fraction: distinguish the normalized primary separation from the actual barycentric radii, and explain why larger mass fractions move Jupiter and Lagrange points inward relative to a fixed unit-separation reference circle.
